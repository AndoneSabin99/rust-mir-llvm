fn main(){
    // - Booleans
    let t : bool = true;
    let f : bool = false;
    // with the usual boolean destructor: if-then-else
    if t == f {
        println!("True is false");
    } else {
        println!("True is not false");
    }
}